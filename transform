def transform_item(item):
    # Define key renames
    key_map = {
        "title": "objective",
        "owner": "ownername",
        "assignee": "assigneename"
    }
    
    # Month abbreviation to full name
    month_map = {
        "Jan": "January", "Feb": "February", "Mar": "March",
        "Apr": "April", "May": "May", "Jun": "June",
        "Jul": "July", "Aug": "August", "Sep": "September",
        "Oct": "October", "Nov": "November", "Dec": "December"
    }
    
    new_item = {}
    
    for key, value in item.items():
        if key == "monthlyUpdates":
            # Flatten monthlyUpdates into individual month keys
            for abbrev, update in value.items():
                full_month = month_map.get(abbrev, abbrev)
                new_item[full_month] = update
        elif key in key_map:
            # Rename the key
            new_item[key_map[key]] = value
        else:
            # Keep as-is
            new_item[key] = value
    
    return new_item


# Transform all items
transformed_data = [transform_item(item) for item in data]
